// Orden de las Operaciones. Es necesario saber que la misma "jerarquía de operaciones" que aplicas o aplicabas en la escuela es la misma que si la aplicabas en programación. Si no te lo sabes, trata de aprenderlo. Te servirá. 

let resultado; 

resultado = (20 + 30) * 2; // veinte mas treinta, y todo ello multiplicado por dos
resultado = (100 + 200 + 300) * 0.2; //Suma de precios de productos y un 20% de descuento.
resultado = 20 + 30 * 2;  // veinte mas treinta por dos
resultado = (600 + 600) *  1.05; // Operacion con paréntesis. 

// console.log(resultado); 



// Incrementos 

let puntaje = 10; 

    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;
    // puntaje--;

// Incremento en 1: ()++
// puntaje++ o ++puntaje

// Decremento en 1: ()--;
// puntaje-- o --puntaje; 

puntaje -= 10; // Sumamos un valor cualquiera que agreguemos [ puntaje += valor ]

// puntaje += sumar valores / puntaje -= restar valores

// puntaje = 100 * 0.5;

console.log(puntaje);